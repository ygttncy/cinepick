export default function AdSlot() {
  return (
    <div
      style={{
        margin: "20px auto",
        maxWidth: 1200,
        padding: "12px 16px",
        border: "1px dashed rgba(255,255,255,.2)",
        borderRadius: 12,
      }}
    >
      {/* Adsense script onaydan sonra aktif edilecek */}
      <small>Reklam alanı (AdSense için yer tutucu)</small>
    </div>
  );
}
